\section{Introduction}
\label{sec:intro}

Agents ask the same small questions thousands of times per session: which
context the next turn sees, which tool fits, whether a command may
run~\cite{jevnote}. A generative model answers in prose with no probability to
threshold; a fixed rule sees only what its author foresaw. A \emph{typed
decision} sits between: the caller states a question, lists the admissible
answers and passes the state, and the model returns a distribution over exactly
those answers, which can be type-checked, thresholded, logged and replayed.

Three planes: the \emph{generative} writes (Zen), the \emph{decision} answers
typed questions over evidence (Kai), and the \emph{policy} is deterministic,
written by people, and final. A decision is a program, not a prompt
(Figure~\ref{fig:program}): Zen reasons inside it, Kai judges, code and solvers
compute, policy and people hold authority, and each run leaves a package.

\input{figures/program}

The baselines are Laya~\cite{laya}, open encoder checkpoints that pack the
question, every option and the state into one sequence, and TypeSafe's hosted
Jev~\cite{typesafejev}. Status tags: \meas{} run by us and written into the
paper by a script from the run's artifact; \reported{} published by others;
\shipped{} on the main branch of \texttt{hanzoai/decision} at the revision on
the title page; \proposed{} designed, not built. A number no run has produced
prints \ph. Contributions:
\begin{enumerate}[leftmargin=1.5em]
  \item \textbf{One model}: a multilingual encoder, adapters for sensor
    streams with their metadata, audio, images and video, and a typed-variable
    decoder, on one checkpoint line trained in stages; the adapters
    \proposed{} (Section~\ref{sec:model}).
  \item \textbf{Factorized choice}: options encoded alone and scored against
    shared evidence by two cross-attention blocks, a law per type, and
    retrieval that keeps the mass it cuts, so option count, order and question
    count are not bounded by one sequence \shipped{} (Section~\ref{sec:factor}).
  \item \textbf{Typed-variable diffusion}: a program's questions as typed
    variables, refined in waves bounded by dependency depth and projected
    exactly onto its rules \shipped; the jointly trained masked-slot model
    \proposed{} (Section~\ref{sec:diffusion}).
  \item \textbf{Policy precedence} as a lattice join, with a proof that
    enforcing a model only adds friction, and
    \textbf{programs and packages} that replay, diff, refresh and say what
    flips a decision, with SysML~v2 import and an engineering graph
    \shipped{} (Sections~\ref{sec:authority} and~\ref{sec:programs}).
  \item \textbf{Benchmarks} of capability, speed and accuracy, each cell wired
    to the artifact that fills it (Section~\ref{sec:evaluation}).
  \item \textbf{Kai in the loop}: a systems evaluation of Kai controlling
    Enso's model, context, tools, reasoning budget and stopping, whose thesis
    is that Kai makes Zen generate less, not faster \proposed{}
    (Section~\ref{sec:enso}).
\end{enumerate}

\paragraph{Results.}
At 100{,}000 options Kai's accuracy is
\res{cap/cardinality/kai/k100000.accall} (Section~\ref{sec:capabilities}). One
question costs \res{cap/questions/kai/n1.p50ms}\,ms natively,
\res{cap/questions/laya/n1.p50ms}\,ms for Laya in Python and
\res{cap/questions/jev/n1.p50ms}\,ms for Jev over the network; 1{,}000
questions on one state, \res{cap/questions/kai/n1000.p50ms}\,ms natively
(p50, Section~\ref{sec:speed}). Macro accuracy over the harness's
\res{kj/wins/of} non-MASSIVE suites is \res{kj/macro/kai/accuracy} (Kai),
\res{kj/macro/laya/accuracy} (Laya) and \res{kj/macro/jev/accuracy} (Jev), Kai
leading Jev on \res{kj/wins/kai/jev}; over MASSIVE's
\res{kj/harness/languages} languages, \res{kj/massive/kai/macroaccuracy},
\res{kj/massive/laya/macroaccuracy} and \res{kj/massive/jev/macroaccuracy}
(Section~\ref{sec:accuracy}). Kai trains on the train splits of the harness's
sources, guarded against its evaluation states; tables mark the overlap, and
Section~\ref{sec:limits} says what is not measured and not won.
