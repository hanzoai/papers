% Architecture: one request path, one kernel source, four backends.
\begin{figure}[t]\centering
\begin{tikzpicture}[
  font=\scriptsize,
  box/.style={draw=hzslate,rounded corners=2pt,align=center,inner sep=3pt,minimum height=6mm},
  api/.style={box,fill=hzblue!12,draw=hzblue},
  sched/.style={box,fill=hzslate!10},
  dsl/.style={box,fill=hzgreen!14,draw=hzgreen},
  be/.style={box,fill=hzslate!8,minimum width=13mm},
  ar/.style={-{Latex[length=1.6mm]},draw=hzslate},
  node distance=3.2mm and 5mm]
  \node[api] (req) {\texttt{/v1} request};
  \node[sched,below=of req] (sch) {scheduler\\{\tiny paged KV $\cdot$ Orca batching $\cdot$ spec-decode}};
  \node[sched,below=of sch] (pipe) {pipeline\\{\tiny GQA flash-SDPA $\cdot$ block-MoE $\cdot$ fused router}};
  \node[dsl,below=of pipe] (dsl) {hanzo-fusion DSL \;{\tiny(auto-fusion, hanzo-cl)}};
  \node[be,below left=6mm and 8mm of dsl] (cuda) {CUDA};
  \node[be,right=of cuda] (metal) {Metal};
  \node[be,right=of metal] (hip) {HIP/ROCm};
  \node[be,right=of hip] (vk) {Vulkan};
  \draw[ar] (req)--(sch); \draw[ar] (sch)--(pipe); \draw[ar] (pipe)--(dsl);
  \draw[ar] (dsl.south)--(cuda.north); \draw[ar] (dsl.south)--(metal.north);
  \draw[ar] (dsl.south)--(hip.north); \draw[ar] (dsl.south)--(vk.north);
\end{tikzpicture}
\caption{One binary. A single kernel source in the hanzo-fusion DSL lowers to CUDA,
Metal, HIP, and SPIR-V; the scheduler and pipeline (paged KV, Orca continuous batching,
prompt-lookup speculation, fused GQA/MoE) are backend-independent above the DSL cut.}
\label{fig:arch}
\end{figure}
